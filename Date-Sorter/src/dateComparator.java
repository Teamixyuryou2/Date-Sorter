import java.util.Comparator;

public class dateComparator implements Comparator <Date212>{
	public int compare(Date212 num1, Date212 num2 ) {
		return num1.compareTo(num2);                      //Sort Data for custom tree map
	}
	//define conditions
	
}
